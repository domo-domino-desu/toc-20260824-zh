computing/prologue/asy/tm_exercises

Using the Turing machine emulator
  computing/src/scheme/prologue/turing-machine.rkt
we can generate use the TM source code to generate the corresponding
.asy files of the tape configurations.  Those files
are here.  Most of them are for exercises but a few are for the
text body.  Use `make' to create them.
