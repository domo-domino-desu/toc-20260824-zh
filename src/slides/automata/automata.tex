\RequirePackage{xr-hyper}  % for cleveref; https://tex.stackexchange.com/a/244272/121234 and https://www.latex4technics.com/?note=2uab
\documentclass[9pt,t,serif,professionalfont,xcolor={table,dvipsnames},noamsthm]{beamer}
\PassOptionsToPackage{pdfpagemode=FullScreen}{hyperref}
\setbeamersize{text margin left = 0.5cm,
               text margin right = 0.3cm}
\mode<presentation>
{
  \usetheme{boxes}
  \setbeamercovered{invisible}
  \setbeamertemplate{navigation symbols}{} 
}
\hypersetup{colorlinks=true,linkcolor=blue} 

\usepackage{../presentation,../presentationfonts}
\usepackage{../../grammar}

% load the pdfanim style, should be done after hyperref
% \usepackage{pdfanim}

% load and initialise animation
% arguments:
%   \PDFAnimLoad[options]{name}{xxx}{number}
%   - options: in this example the animation is looped
%   - name: name of the animation
%     (with this name it can be reused several times in the document)
%   - number: number of animation frames / files (n)
% the animation will be composed of files
% xxx0.pdf, xxx1.pdf ... xxx(n-1).pdf


\title{Automata}

\author{Jim Hef{}feron}
\institute{
  Mathematics and Statistics\\
  Saint Michael's College\\
  Colchester, VT\\[1ex]
  \texttt{hefferon.net}
}
\date{}

% This is only inserted into the PDF information catalog.
\subject{Automata}

\usepackage{catchfilebetweentags} % read text from background.tex
\newcommand{\catchfilefn}{../../automata/automata.tex}
% Keep from swallowing end-of-lines
%   https://tex.stackexchange.com/a/40704/121234
\usepackage{etoolbox}
\makeatletter
\patchcmd{\CatchFBT@Fin@l}{\endlinechar\m@ne}{}
  {}{\typeout{Unsuccessful patch!}}
\makeatother

\usepackage{xr}
  % \externaldocument{../../book}
  \externaldocument{../../automata/automata}
\usepackage{cleveref}

% Directory of this chapter's asy material
\newcommand{\asydir}{../../automata/asy/}
% \newcommand{\tmexercises}{\asydir tm_exercises/pdfs/}
% Directory of the src/asy material
\newcommand{\generalasydir}{../../asy/}

% \usepackage{multirow,bigstrut}
\begin{document}
\begin{frame}
  \titlepage
\end{frame}




\section{Finite State machines}
% ------------------------------------------------------
\begin{frame}
  \frametitle{Motivation}

Turing and other researchers tried to capture what is computable in principle.
Church's Thesis is that their definitions do indeed capture the intuitive 
idea of effectively computable, of computable by an unbounded mechanism.

A person can object to the characterization,
``Although each Turing machine has only finitely many states, it can write to
the tape.
We can create a Turing machine that keeps writing characters
to new places on the tape, and so has unboundedly many configurations.
No physical device can have unboundedly many configurations.''

We will next study machines that are modifications of Turing machines.
These have finitely many states and can read,
but they cannot write.
All they can do is consume their input and then either indicate that the 
input string was accepted or not accepted. 
\end{frame}



\begin{frame}
  \frametitle{Definitions}
\ExecuteMetaData[\catchfilefn]{def:FSM}

\begin{center}
  \includegraphics{\generalasydir share/machine02.pdf}
\end{center}
Our convention is that the start state is $q_0$.

\pause{\color{blue}\textit{Remark:}}
Turing machines are defined to be
sets of instructions  $q_pT_pT_nq_n$.
The instructions are subject to the restriction that the set must be
deterministic, that no two instructions can begin with 
the same pair $q_pT_p$.
Consequently, over all $q_pT_pT_nq_n\in\TM$
the association of present pair $q_pT_p$ with next pair~$T_nq_n$,
defines a function, the next-state function
$\map{\Delta}{Q\times \Sigma}{\Sigma\cup\set{\str{L},\str{R}}\times Q}$.  

Finite State machines work the same way, 
and so in the definition above 
we took the short cut of going straight to the function.
\end{frame}


\begin{frame}
\begin{example}
This machine has input alphabet 
$\Sigma=\B=\set{\str{0},\str{1}}$.
The set of states is $Q=\set{q_0,q_1,q_2}$
It has one accepting state $F=\set{q_2}$.
It will accept strings that contain at least two \str{1}'s.
\begin{center}
  \includegraphics{asy/fsm00.pdf}
\end{center}
The transition function is a tabular version of the diagram.
For instance, $\Delta(q_1,\str{1})=q_2$.
\begin{center}
\begin{transitionfunction}
      &\str{0}    &\str{1}   \\
    \cline{2-3}
    $q_0$ &$q_0$ &$q_1$   \\
    $q_1$ &$q_1$ &$q_2$    \\
    \acceptingstate
    $q_2$ &$q_2$ &$q_2$  \\
\end{transitionfunction}
\end{center}
The $\acceptingstatemark$ next to $q_2$ marks
it as an accepting state.

This machine accepts the string \str{01010}.
It does not accept the string \str{001}.
\end{example}
\end{frame}


\begin{frame}
\begin{example}
\ExecuteMetaData[\catchfilefn]{ex:FSAValidIntegers}
\begin{center}  \small
  \vcenteredhbox{\includegraphics{\asydir fsa/fsa07.pdf}}
  \hspace{3em}
  \begin{transitionfunction}
      &\str{+}, \str{-} &\str{0}, \ldots{} \str{9} &\tabulartext{other}   \\
    \cline{2-4}
    $q_0$ &$q_1$ &$q_2$ &$e$   \\
    $q_1$ &$e$   &$q_2$ &$e$   \\
    \acceptingstate $q_2$ &$e$   &$q_2$ &$e$   \\
    $e$   &$e$   &$e$   &$e$   \\
  \end{transitionfunction}
  \end{center}
\end{example}
\pause
\begin{example}
This machine accepts only the single string \str{HEF}.
\begin{center}
  \includegraphics{asy/fsm01.pdf}
\end{center}
\end{example}
\end{frame}


\begin{frame}
\begin{example}
This machine accepts only strings where \str{HEF} is the prefix.
\begin{center}
  \includegraphics{asy/fsm02.pdf}
\end{center}
\end{example}
\pause
\begin{example}
This machine accepts only strings where \str{HEF} is the suffix.
\begin{center}
  \includegraphics{asy/fsm03.pdf}
\end{center}
\end{example}
\end{frame}




\begin{frame}
\begin{example}
This machine accepts only strings where \str{HEF} is a substring.
\begin{center}
  \includegraphics{asy/fsm04.pdf}
\end{center}
\end{example}
\pause
\begin{example}
This machine accepts only strings consisting of
zero or more repetitions of \str{HEF}.
\begin{center}
  \includegraphics{asy/fsm05.pdf}
\end{center}
\end{example}
\end{frame}




\begin{frame}
\begin{example}
This machine accepts all strings starting with zero or more 
copies of \str{HEF}.
\begin{center}
  \includegraphics{asy/fsm06.pdf}
\end{center}
(It is a gotcha question; all strings begin with zero-many copies of \str{HEF}.)
\end{example}
\pause
\begin{example}
This machine accepts only the strings \str{CAT} or \str{DOG}.
\begin{center}
  \includegraphics{asy/fsm07.pdf}
\end{center}
\end{example}
\end{frame}


\begin{frame}
\begin{example}
This machine, with input alphabet $\sigma=\set{\str{a},\str{b}}$,
accepts only strings with an even number of \str{a}'s.
\begin{center}
  \includegraphics{asy/fsm08.pdf}
\end{center}
\end{example}
\pause
\begin{example}
This machine, with the same alphabet, 
accepts strings with at least three \str{a}'s.
\begin{center}
  \includegraphics{asy/fsm09.pdf}
\end{center}
\end{example}
\end{frame}



% \begin{frame}{Configurations}
% \ExecuteMetaData[\catchfilefn]{discussion:FSMConfiguration}
% \end{frame}

\begin{frame}{The action of a Finite State machine}
As with Turing machines, we can give a definition
of how Finite State machines act that is mathematically precise.
A \definend{configuration} of a Finite State machine $\FSM$ is 
a pair $\mathcal{C}=\sequence{q,\tau}$, with
$q\in Q$ and $\tau\in\kleenestar{\Sigma}$.
Where $\mathcal{C}_0=\sequence{q_0,\tau_0}$ is the
\definend{initial configuration},
$\tau_0$ is the \definend{input}.

\begin{example}
Give this machine the input $\tau_0=\str{abababb}$.
\begin{center}
  \includegraphics{asy/fsm09.pdf}
\end{center}
The transition function~$\Delta$ determines how
the machine moves step-by-step,
in response to the input (read `$\yields$' aloud as ``yields'').
\begin{align*}
    \mathcal{C}_0=\sequence{q_0,\str{abababb}}
    &\yields \mathcal{C}_1=\sequence{q_1,\str{bababb}}  \\
    &\yields \mathcal{C}_2= \sequence{q_1,\str{ababb}}  \\
    &\yields \mathcal{C}_3= \sequence{q_2,\str{babb}}  \\
    &\yields \mathcal{C}_4 = \sequence{q_2,\str{abb}}   \\
    &\yields \mathcal{C}_5 = \sequence{q_3,\str{bb}}  \\
    &\yields \mathcal{C}_6 = \sequence{q_3,\str{b}}  \\
    &\yields \mathcal{C}_7=  \sequence{q_3,\emptystring}
\end{align*}
The machine accepts $\tau_0$ because when $\tau=\emptystring$ 
then the state~$q_3$ is accepting.
\end{example}
\end{frame}



\begin{frame}{Language of a machine}
\ExecuteMetaData[\catchfilefn]{def:FSMLanguage}

\begin{example}
This machine.
\begin{center}
  \includegraphics{asy/fsm07.pdf}
\end{center}
has the language
$\lang(M)=\set{\str{CAT},\str{DOG}}$.
\end{example}
\end{frame}

\begin{frame}
\begin{example}
This machine 
\begin{center}
  \includegraphics{asy/fsm05.pdf}
\end{center}
Has the language
$\lang=\set{\emptystring,\str{HEF}, \str{HEFHEF}, \str{HEF}^3,\ldots}$
\end{example}

\begin{example}
This machine recognizes a natural number
that is a multiple of three (that is, it accepts strings 
representing such a number in base 10, such as \str{15} or~\str{5013}.
\begin{center}
  \vcenteredhbox{\includegraphics{\asydir fsa/fsa12.pdf}}
\end{center}
The language is this, where $\Sigma=\set{\str{0}, \ldots\,\str{9}}$.
\begin{equation*}
  \lang(\FSM)=\set{\sigma\in\kleenestar{\Sigma}
    \suchthat\text{$\sigma$ is the decimal representation of a multiple of three}}
\end{equation*}
\end{example}
\end{frame}

\begin{frame}[fragile]{Finite State machines translate easily to code}
\vspace*{-1ex}
Here is a Python implementation of the multiple of three machine.
\begin{lstlisting}
def multiple_of_three_fsm(s):
    state = 0
    for ch in s:
        if state == 0:
            if ch in set([0,3,6,9]):
                state = 0
            elif ch in set([1,4,7]):
                state = 1
            else:
                state = 2
        elif state == 1:
            if ch in set([0,3,6,9]):
                state = 1
            elif ch in set([1,4,7]):
                state = 2
            else:
                state = 0
        else:
            if ch in set([0,3,6,9]):
                state = 2
            elif ch in set([1,4,7]):
                state = 0
            else:
                state = 1
    if state == 0:
        return True
    else:
        return False      
\end{lstlisting}
\end{frame}

\begin{frame}{Extended transition function}
\ExecuteMetaData[\catchfilefn]{def:ExtendedTransitionFcn}

\begin{example}
For this machine, compute the extended transition function 
on all strings of length less than three.
\begin{center}
  \includegraphics{asy/fsm09.pdf}
\end{center}
The alphabet is $\Sigma=\set{\str{a},\str{b}}$ so
there are seven string  of length less than three.
\begin{center}
\begin{tabular}{r|*{7}{c}}
  \tabulartext{Input $\sigma$}
    &$\emptystring$ &\str{a} &\str{b} &\str{aa} &\str{ab} &\str{ba} &\str{bb} \\
  \cline{2-8}
  \tabulartext{$\hat{\Delta}(\sigma)$}
    &$q_0$          &$q_1$   &$q_0$   &$q_2$    &$q_1$    &$q_1$    &$q_0$
\end{tabular}
\end{center}
\end{example}
\end{frame}







% ===========================================
\section{Nondeterminism}
\begin{frame}{Recall: deterministic computation} 
The definition gives that a Turing machine is a
sets of instructions  $q_pT_pT_nq_n$.
These sets are subject to the restriction of determinism, 
that no two instructions can begin with the same pair, $q_pT_p$.
Consequently, over the $q_pT_pT_nq_n\in\TM$,
the association of present pair $q_pT_p$ with next pair~$T_nq_n$,
defines a function, the next-state function
$\map{\Delta}{Q\times \Sigma}{\Sigma\cup\set{\str{L},\str{R}}\times Q}$. 
It is this function that determines the sequence of transitions
from configuration to configuration.   

Finite State machines work the same way. 
(In fact, having gone through the development of Turing machines already, 
we didn't define Finite State machines via instructions 
but instead went straight to the next-state function.)

\pause
We now drop the restriction of determinism.
We will 
do it here for Finite State machines and in the next chapter
for Turing machines.

Nondeterminism proves to be a very important idea, but it
can give the initial impression of being odd or unnatural.
So before the definition 
we will see two examples of computations that, in some sense,
do more than one thing at a time.
These will lead to two mental models for what is happening.
\end{frame}


\begin{frame}{Deriving a string from a grammar}
From this grammar
\begin{grammar}
  S \produces{} T\trm{b}U

  T \produces{} \trm{a}T \syntaxor \emptystring{}

  U \produces{} \trm{a}U \syntaxor \trm{b}U \syntaxor \emptystring{}  
\end{grammar}
we can derive, for example, $\sigma_0=\str{aabab}$
  \begin{center}
  \begin{tabular}{l}
    S \derives T \trm{b} U
    \derives \trm{a} T \trm{b} U    
    \derives \trm{a} \trm{a} T \trm{b} U    
    \derives \trm{a} \trm{a} \emptystring{} \trm{b} U
       =\trm{a} \trm{a} \trm{b} U    
    \\ \quad 
    \derives \trm{a} \trm{a} \trm{b} \trm{a} U
    \derives \trm{a} \trm{a} \trm{b} \trm{a} \trm{b} U
    \derives \trm{a} \trm{a} \trm{b} \trm{a} \trm{b} \emptystring{}
    =\trm{a} \trm{a} \trm{b} \trm{a} \trm{b}
  \end{tabular}
  \end{center}
or $\sigma_1=\str{baab}$.
\begin{center}
  \begin{tabular}[t]{l}
    S\derives  T \trm{b} U
    \derives \emptystring{} \trm{b} U
      = \trm{b} U    
    \derives \trm{b} \trm{a} U 
    \\ \quad 
    \derives \trm{b} \trm{a} \trm{a} U 
    \derives \trm{b} \trm{a} \trm{a} \trm{b} U 
    \derives  \trm{b} \trm{a} \trm{a} \trm{b} \emptystring{}  
    =  
    \trm{b} \trm{a} \trm{a} \trm{b} 
  \end{tabular}
\end{center}
\pause
Derivation is a computation in that it is symbol pushing, following
effective rules.
It is nondeterministic in that while doing a sequence of $\derives$~steps,
we often find that more than one rule applies,
so there is not just a unique next string.
\end{frame}


% to get all three images to line up I need to know the width of the largest
\newsavebox\jhimagebox %
\sbox{\jhimagebox}{%
  \includegraphics{asy/nfsm19.pdf}%
}% 
\setlength{\unitlength}{\wd\jhimagebox}
\begin{frame}
  % \frametitle{Finding a derivation}
So, suppose that we have a grammar where there is sometimes more than one
applicable rule.
Give a target string~$\sigma$, how to derive it?

One way is brute force:~perform
all applicable production rules to the start symbol, 
then all applicable rules to each result,
etc. 
If $\sigma$ is in the language of the grammar then we will 
eventually find it.
This is a breadth-first search of the $\derives\!$~sequences;
here are the first few levels.
% The width is \the\wd\jhimagebox.
% The height is \the\ht\jhimagebox.
% The width is 325.72612pt. The height is 110.4624pt.
% I by-hand calculated that 110.4624÷325.72612 = 0.339126626
\begin{center}\small
  \begin{picture}(1,0.339126626)
    \only<1>{\put(0.558,0.249){\includegraphics{asy/nfsm17.pdf}}}%
    \only<2>{\put(0.124,0.187){\includegraphics{asy/nfsm18.pdf}}}%
    \only<3-4>{\put(0,0){\includegraphics{asy/nfsm19.pdf}}}%
  \end{picture}
\end{center}
\only<4>{The second way, 
what we actually do to solve these problems, is to guess.
Then we verify that the guess is right.}
\end{frame}


\begin{frame}{A nondeterministic machine}\vspace*{-1ex}
This machine is nondeterministic: for a given present-state, input character 
pair there may be one next state, or more than one, or none.
For instance, leaving $q_0$ are two different arrows labeled~\str{0}.
And leaving $q_1$ is no arrow at all labeled~\str{1}.\vspace*{-1ex}
\begin{center}\small
  \vcenteredhbox{\includegraphics{\asydir nfsm/nfsm00.pdf}}
\end{center}
Suppose that this machine gets the input~\str{00001}.
Just as with the grammar, we will have two ways to think of what happens.
First, we can trace out every possible branch.
\end{frame}
\begin{frame}
\begin{center}\small
  \vcenteredhbox{\includegraphics{\asydir nfsm/nfsm00.pdf}}
\end{center}
% The input is~\str{00001}.
\begin{center}\small
  \only<1>{\includegraphics{asy/nfsm02.pdf}}%
  \only<2>{\includegraphics{asy/nfsm03.pdf}}%
  \only<3>{\includegraphics{asy/nfsm04.pdf}}%
  \only<4>{\includegraphics{asy/nfsm05.pdf}}%
  \only<5>{\includegraphics{asy/nfsm06.pdf}}%
  \only<6>{\includegraphics{asy/nfsm07.pdf}}%
  \only<7>{\includegraphics{asy/nfsm08.pdf}}% using onslide causes other figs to not be centered
\end{center}
\only<7>{The highlighted branch ends in an accepting state.}
\end{frame}

\begin{frame}
As with the grammar,
we are picturing that when the machine is called on to do two things, 
it does them both.
So the computation history is not straight-line but instead is a tree.
And, when there is no next state, the branch dies.

After the first step, the machine is in multiple states simultaneously, namely 
$q_0$ and~$q_1$.
The same also happens at all later steps; in all of those steps
the machine is in a set of states.

There is a branch that gets to the end and accepts the 
input string~\str{00001}.
Branches can fail to accept the string because they die,  
or because they get to the end and they just are not in an accepting state.

\pause
Also as with the grammar, 
there is a second way to think of what is happening. 
This machine, when presented with an input string, must decide:~when 
should it stop going around $q_0$'s loop 
and start following the tail to the right?
Above we've seen the machine accepting \str{00001},
so it has solved the problem that we gave it.
That is, 
we could say that the machine has correctly guessed.
\end{frame}




\begin{frame}{First mental model for nondeterminism: spawning}
For the grammar example, we made a distinct branch each time more than
one production rule applied to a string.
For the machine, we also branched each time there was more than one
possible thing to do.

Another example of this is 
the Travelling Salesman problem.
Suppose that we want a cyclic trip 
that visits every state capitol in the forty eight contiguous US~states
in less than $16\,000$ kilometers.
This is nondeterminism in that if we start at Montpelier, Vermont 
then there are multiple possible next cities
and it is not clear which is the right one. 
One natural approach is to spawn forty seven child processes, 
a child for each potential next city.
The process assigned Albany, 
for instance, would know that the trip so far is $126$~kilometers. 
After that, each child would fork its own set of children, forty six of them.
At the end if there is
a trip of less than $16\,000$ kilometers then some process has found it.
Note that we consider
the overall computation a success if there exists 
even one process that is a success.

This model 
is a natural approach when called on to do multiple things.
We are all used to devices with lots of windows open on the screen,
doing lots of things.
Under the hood, the operating system forks child processes to cover each
thing.
\end{frame}


\begin{frame}{Second mental model for nondeterminism: guessing}
For both the grammar example and the machine example, we also framed
that the system guessed the answer.

For example, we described the nondeterministic machine as guessing when 
to stop going around $q_0$'s loop, 
and start following the tail to the right.

This characterization is jarring because 
our previous experience with computers in programming class is that
they don't guess.
An alternate way to say it is that we imagine that the machine is
furnished with the answer (``go around twice, then off to the right'') and
only has to check that answer.
Often, the furnisher is personified as a demon.
\pause
\begin{center}\small
  \vcenteredhbox{\includegraphics[height=1.2in]{../../automata/pix/flauros.jpg}}
  \\
  \mbox{Flauros, the Duke of Hell.}
\end{center}
\end{frame}

\begin{frame}{What the two views share}
In both mental models, the computation succeeds if there is a way for it 
to succeed.
In particular, 
the machine accepts the input if there exists a sequence of configurations
that ends in an accepting state.

Very important: `if there exists' doesn't mean that there must be an algorithm
for finding it such as backtracking or dynamic programming, etc.
This is pure existence\Dash it just requires that there 
exist a way for the computation to succeed.
That's why the demon is here;~it is a being so powerful that it 
goes beyond needing ordinary computation to know the
answer.
\end{frame}


\begin{frame}
\frametitle{Definition of a Nondeterministic Finite Sate machine}

We change one thing from the definition of Finite State machine,
the codomain of $\Delta$.

\ExecuteMetaData[\catchfilefn]{def:NondeterministicFiniteStateMachine}
\ExecuteMetaData[\catchfilefn]{def:NFSMAccepted}
\pause
\begin{example}
This nondeterministic
garage door listener waits to hear the remote control send
\str{0101110}.
\begin{center}
  \includegraphics{\asydir nfsm/nfsm11.pdf}
\end{center}
That is, it accepts the language
$\set{\sigma\concat\str{0101110}\suchthat \sigma\in\kleenestar{\B}}$.

For example, this listener will end in an accepting 
state if it 
hears a string $\sigma=\str{010101110}$, with three \str{01}'s in the prefix
instead of two.
In terms of our second framing, it guesses that it should ignore
the first \str{01} but not the second.
(Said more precisely, there exists a sequence of configuations that leads
to acceptance of $\sigma$.)
\end{example}

We will show how to convert any nondeterministic Finite State
machine into a deterministic machine,
so we can think of this machine
as an abbreviation for a deterministic one.
This may help obviate head-scratching over `guessing'.
\end{frame}

\begin{frame}
  \frametitle{Nondeterminism can make a simpler machine}
\begin{example}
This nondeterministic Finite State machine 
accepts only those strings ending in \str{HEF}.
(The alphabet~$\Sigma$ is the capital ASCII letters.)
\begin{center}
  \includegraphics{asy/nfsm11.pdf}
\end{center}

\pause
Contrast that with what we got when we did this job
using a deterministic machine.
\begin{center}
  \includegraphics{asy/fsm03.pdf}
\end{center}
The nondeterministic machine is
more obviously true to its specification.
\end{example}
\end{frame}


\begin{frame}
\begin{example}
This nondeterminstic Finite State machine~$\FSM$  
with alphabet $\Sigma=\set{\str{a},\str{b}}$
accepts an input string only
if it has \str{a} in the third position from the end.
\begin{center}
  \includegraphics{asy/nfsm09.pdf}
\end{center}
Thus 
$\lang(\FSM)=\set{\str{aaa}, \str{aab}, \str{aba}, \str{abb},\str{aaaa}, \str{aaab},\str{aaba},\str{aabb},\str{baaa},\str{baab},\ldots}$.
\end{example}

\pause
\begin{example}
This machine~$\hat{\FSM}$ with alphabet $\Sigma=\set{\str{0},\str{1}}$ 
accepts an input string only
if it contains a substring
with a \str{1} followed by any number of \str{0}'s
including zero-many \str{0}'s, 
followed by a second \str{1}.
\begin{center}
  \includegraphics{asy/nfsm16.pdf}
\end{center}
Thus 
$\lang(\hat{\FSM})=\set{\str{11}, \str{011}, \str{110}, \str{111},\str{0011}, \str{0101},\str{0110},\str{0111},\ldots}$.
\end{example}
\end{frame}

\begin{frame}{$\varepsilon$ transitions}
\ExecuteMetaData[\catchfilefn]{discussion:epsilonTransitions}

\pause
These examples show what this extension can do.
\begin{example}
\hypertarget{ex:EpsilonTransitionsForConcat}{The machine on the left} 
accepts strings with 
prefix \str{ab}.
The one on the right accepts strings with suffix \str{ab}.
\begin{center}
  \vcenteredhbox{\includegraphics{asy/nfsm12.pdf}}
  \hspace{4em}
  \vcenteredhbox{\includegraphics{asy/nfsm13.pdf}}
\end{center}
This $\varepsilon$ transition joins them together, serially.
\begin{center}
  \vcenteredhbox{\includegraphics{asy/nfsm14.pdf}}
\end{center}
\end{example}
The joined machine accepts strings that begin with 
\str{ab} and end with \str{ab}.
\end{frame}


\begin{frame}
\begin{example} 
\hypertarget{ex:EpsilonTransitionsForOr}{We can take} the same two machines, 
one that accepts strings with 
prefix \str{ab} and
one that accepts strings with suffix \str{ab}.
\begin{center}
  \vcenteredhbox{\includegraphics{asy/nfsm12.pdf}}
  \hspace{4em}
  \vcenteredhbox{\includegraphics{asy/nfsm13.pdf}}
\end{center}
and join them in parallel.
The result accepts strings that begin with \str{ab} 
or end with \str{ab}.
\begin{center}
  \vcenteredhbox{\includegraphics{asy/nfsm15.pdf}}
\end{center}
\end{example}

\pause
\begin{example} 
\hypertarget{ex:EpsilonTransitionsForStar}{An $\varepsilon$ transition edge} 
can also take a machine and
give it the ``any number of repetitions'' effect.
That is, without that edge this machine accepts only two strings, the empty
string or the string \str{ab}.
With the edge, the lanaguage is  
$\set{(\str{ab})^n\suchthat n\in\N}$.
\begin{center}
  \includegraphics{\asydir nfsm/nfsm15.pdf}
\end{center}
\end{example}
\end{frame}


\begin{frame}{Equivalence of the machine types}
\ExecuteMetaData[\catchfilefn]{th:dfsmEquivndfsm}

\pause
\begin{example}
Here is a nondeterministic Finite State machine.
\begin{center}
  \includegraphics{asy/nfsm20.pdf}
\end{center}
This is an equivalent deterministic Finite State machine.
\begin{center}\small
\begin{tabular}{r|cc}
  \multicolumn{1}{c}{\ } &\str{0} &\str{1}  \\
  \cline{2-3}
  $s_0=\set{}$            &$s_0$ &$s_0$  \\
  $s_1=\set{q_0}$         &$s_1$ &$s_4$  \\
  $s_2=\set{q_1}$         &$s_0$ &$s_5$  \\
  \acceptingstate
  $s_3=\set{q_2}$         &$s_3$ &$s_0$  \\
  $s_4=\set{q_0,q_1}$     &$s_1$ &$s_7$  \\
  \acceptingstate
  $s_5=\set{q_0,q_2}$     &$s_5$ &$s_4$  \\
  \acceptingstate
  $s_6=\set{q_1,q_2}$     &$s_3$ &$s_5$  \\
  \acceptingstate
  $s_7=\set{q_0,q_1,q_2}$ &$s_5$ &$s_7$  \\
\end{tabular}
\end{center}
\end{example}
\end{frame}




\begin{frame}
What if there are $\varepsilon$ transistions?

\ExecuteMetaData[\catchfilefn]{df:EpsilonClosure}

\begin{example}
For this machine,
these are the $\varepsilon$~closures.
\begin{center}
  \vcenteredhbox{\includegraphics{../../automata/asy/nfsm/nfsm93.pdf}}
  \quad
  \begin{tabular}{r|*{4}{c}}
  \tabulartext{state}
     &$q_0$           &$q_1$          &$q_2$        &$q_3$  
  \\ \cline{2-5}
  \tabulartext{$\varepsilon$ closure}
     &$\set{q_0,q_1}$ &$\set{q_1}$ &$\set{q_1,q_2,q_3}$ &$\set{q_1,q_3}$ 
\end{tabular}
\end{center}
\end{example}

\pause
\ExecuteMetaData[\catchfilefn]{df:DFSMFromNFSM}
\end{frame}

\begin{frame}
As an example we find $\Delta_{\mathcal{D}}(\set{q_2},1)$.
First, $A=\hat{E}(\set{q_2})=\set{q_1,q_2,q_3}.$
Applying $\Delta_N$ with input $\str{1}$ to each of $A$'s members 
gives $B=\set{q_1,q_2}$.
Then $\hat{E}(B)=\set{q_1,q_2,q_3}=s_{14}$.
\begin{center}\small
  \vcenteredhbox{\includegraphics{../../automata/asy/nfsm/nfsm93.pdf}} 
  \hspace*{2em}
\begin{tabular}{r|cc}
  \multicolumn{1}{r}{\ }        
                                &\str{0}          &\str{1}  \\
  \cline{2-3}
  $s_0=\set{}$                  &\only<2>{$s_{0}$} &\only<2>{$s_{0}$}  \\
  $s_1=\set{q_0}$               &\only<2>{$s_{15}$} &\only<2>{$s_{5}$}  \\
  \acceptingstate
  $s_2=\set{q_1}$               &\only<2>{$s_{5}$} &\only<2>{$s_{2}$}  \\
  $s_3=\set{q_2}$               &\only<2>{$s_{12}$} &\only<2>{$s_{14}$}  \\[0.75ex]
  $s_4=\set{q_3}$               &\only<2>{$s_{12}$} &\only<2>{$s_{2}$}  \\
  \acceptingstate
  $s_5=\set{q_0,q_1}$           &\only<2>{$s_{15}$} &\only<2>{$s_{5}$}  \\
  $s_6=\set{q_0,q_2}$           &\only<2>{$s_{15}$} &\only<2>{$s_{15}$}  \\
  $s_7=\set{q_0,q_3}$           &\only<2>{$s_{15}$} &\only<2>{$s_{5}$}  \\[0.75ex]
  \acceptingstate
  $s_8=\set{q_1,q_2}$           &\only<2>{$s_{12}$} &\only<2>{$s_{14}$}  \\
  \acceptingstate
  $s_9=\set{q_1,q_3}$           &\only<2>{$s_{12}$} &\only<2>{$s_{2}$}  \\
  $s_{10}=\set{q_2,q_3}$        &\only<2>{$s_{12}$} &\only<2>{$s_{14}$}  \\
  \acceptingstate
  $s_{11}=\set{q_0,q_1,q_2}$    &\only<2>{$s_{15}$} &\only<2>{$s_{15}$} \\[0.75ex]
  \acceptingstate
  $s_{12}=\set{q_0,q_1,q_3}$    &\only<2>{$s_{15}$} &\only<2>{$s_{5}$}  \\
  $s_{13}=\set{q_0,q_2,q_3}$    &\only<2>{$s_{15}$} &\only<2>{$s_{15}$}  \\
  \acceptingstate
  $s_{14}=\set{q_1,q_2,q_3}$    &\only<2>{$s_{12}$} &\only<2>{$s_{14}$}  \\
  \acceptingstate
  $s_{15}=\set{q_0,q_1,q_2,q_3}$ &\only<2>{$s_{15}$} &\only<2>{$s_{15}$}  \\
\end{tabular}
\end{center}
The start state is $\hat{E}(\set{q_0})=\set{q_0, q_1}=s_5$.
The only accepting state of $\mathcal{N}$ is $q_1$ and its $\varepsilon$~closure
is $\set{q_1}$, so a state of $\mathcal{D}$ is accepting if and only if
it contains $q_1$.
\end{frame}





% =======================================================
\section{Regular expressions}

\begin{frame}{Examples}
There is a pattern to the languages accepted by a Finite State machine.
We describe them with \alert{regular expressions}.
We will show some examples first, then a definition.

\pause
\ExecuteMetaData[\catchfilefn]{ex:RegexOne}

\pause
\ExecuteMetaData[\catchfilefn]{ex:RegexTwo}

\pause
\begin{example}
The strings that are accepted by this machine match \re{a*ba*}.
\begin{center}
  \includegraphics{\asydir fsa/fsa14.pdf}
\end{center}
\end{example}
\end{frame}


\begin{frame}{Definition: syntax}
\begin{definition}
\ExecuteMetaData[\catchfilefn]{def:RESyntax}
\end{definition}
\end{frame}

\begin{frame}{Semantics}
\ExecuteMetaData[\catchfilefn]{discussion:RESemantics}
\end{frame}


\begin{frame}{Examples}
\ExecuteMetaData[\catchfilefn]{ex:RegexThree}
\pause
\ExecuteMetaData[\catchfilefn]{ex:RegexFour}
\pause
\begin{example}
\ExecuteMetaData[\catchfilefn]{ex:RegexFive}
\end{example}
\pause
\ExecuteMetaData[\catchfilefn]{ex:RegexSix}
\end{frame}


\begin{frame}{More examples}

\begin{example}
Produce a regular expression to match each description.
For each, take the alphabet to be the set of bits,~$\B=\set{\str{0},\str{1}}$.
\begin{enumerate}
\item
The set of bitstrings that end in three consecutive \str{1}'s. 
\pause
\item
The set of bitstrings with at least one~\str{0}.
\pause
\item The bitstrings containing at least three~\str{1}'s.
\pause
\item The set of bitstrings starting with a digit other than the one
that it ends with.
\end{enumerate}
\end{example}

% \begin{example}
% Produce a regular expression for each.
% Take the alphabet to be the lower-case ASCII letters.
% Use \url{http://joshua.smcvt.edu/cgi-bin/test_regex.py}.
% (1)~Words containing both an \str{e} and a \str{u}.
% \pause
% (2)~Words having \str{f} as the second letter.
% \pause
% (3)~How many words start in \str{th}?
% \pause
% (4)~Words starting with \str{w} and containing a \str{y}.
% \end{example}
\end{frame}
% import re
% import sys

% file = open(sys.argv[2], "r")

% for line in file:
%      if re.search(sys.argv[1], line):
%          print line,

\begin{frame}{Kleene's Theorem}
\ExecuteMetaData[\catchfilefn]{th:KleenesTheorem}

\pause
\ExecuteMetaData[\catchfilefn]{lem:KleenesThmRegexToMachine}

\proofname\hspace{0.6em} 
We will show that for any regular expression~$R$ there is a machine that
accepts strings matching that expression.
We give nondeterministic machines, but of course we can convert them
to deterministic ones so we can do that without loss of generality.

\ExecuteMetaData[\catchfilefn]{pf:KleenesThmRegexToMachinei}
  \begin{center}
    \hspace*{8em}
    \includegraphics{\asydir nfsm/nfsm32.pdf}
    \hspace{2em plus 1fill}
    \includegraphics{\asydir nfsm/nfsm33.pdf}
    \hspace{2em plus 1fill}
    \includegraphics{\asydir nfsm/nfsm34.pdf}
    \hspace*{8em}
  \end{center}
\end{frame}

\begin{frame}
\ExecuteMetaData[\catchfilefn]{pf:KleenesThmRegexToMachineiia}
See \hyperlink{ex:EpsilonTransitionsForOr}{this earlier example of parallel machines}.

\ExecuteMetaData[\catchfilefn]{pf:KleenesThmRegexToMachineiib}
See \hyperlink{ex:EpsilonTransitionsForConcat}{the example of machine concatenation}.

\ExecuteMetaData[\catchfilefn]{pf:KleenesThmRegexToMachineiic}
See \hyperlink{ex:EpsilonTransitionsForStar}{the example of machine starring}.
\qedsymbol
\end{frame}



\begin{frame}
\ExecuteMetaData[\catchfilefn]{lem:KleenesThmMachineToRegex}

Here is the idea:~start with a Finite State machine and eliminate its states
one at a time, keeping the accepted language the same.
\begin{center}
\includegraphics{\asydir nfsm/nfsm35.pdf}
\hspace{2em}
\includegraphics{\asydir nfsm/nfsm36.pdf}
\end{center}
For this, we generalize the transition graphs to allow
edge labels that are regular expressions.

A bit more detail:~take \re{a}, \re{b}, \re{c}, and~\re{d} to be
regular expressions.
Start by introducing a new start state guaranteed to have
no incoming edges, $e$,
and a new final state guaranteed to be unique,~$f$.
\begin{center}
  \vcenteredhbox{\includegraphics{\asydir nfsm/nfsm64.pdf}}
  \hspace{2em}
  \vcenteredhbox{\includegraphics{\asydir nfsm/nfsm65.pdf}}
\end{center}
Then eliminate $q_1$ as below.
\begin{center}
  \vcenteredhbox{\includegraphics{\asydir nfsm/nfsm66.pdf}}
\end{center}
\end{frame}



\begin{frame}
The proof is in the book.
Here is an illustration. 
\begin{example}
Consider~$\FSM$ on the left.
Introduce $e$ and~$f$ to get $\hat{\FSM}$ on the right.
\begin{center}
  \vcenteredhbox{\includegraphics{\asydir nfsm/nfsm48.pdf}}
  \hspace{2em}
  \vcenteredhbox{\includegraphics{\asydir nfsm/nfsm49.pdf}}
\end{center}
\pause
Start by eliminating $q_2$.
That gives this machine.
\begin{center}
  \vcenteredhbox{\includegraphics{\asydir nfsm/nfsm50.pdf}}
\end{center}
\end{example}
\end{frame}
\begin{frame}
Next eliminate $q_1$.
\begin{center}
  \vcenteredhbox{\includegraphics{\asydir nfsm/nfsm51.pdf}}    
\end{center}
\pause
Finally, eliminate $q_0$.
\begin{center}
  \vcenteredhbox{\includegraphics{\asydir nfsm/nfsm52.pdf}}    
\end{center}
That regular expression simplifies.
For instance, \re{a$\varepsilon$}=\re{a}.
The result is \re{(a(b|ab*b))*($\varepsilon$|a)}.
\end{frame}




% ==============================================
\section{Regular languages}

\begin{frame}{Definition}
\ExecuteMetaData[\catchfilefn]{def:RegularLangs}

\begin{example}
The set
$\set{\str{a}\str{b}^n\suchthat n\in\N}
  =\set{\str{a}, \str{ab}, \str{abb}\,\ldots}$ is a regular language.
Another language that is regular is any finite set of strings. 
\end{example}

\pause
In the proof of Kleene's Theorem, we saw that if two languages
are regular then their union is also regular.
We say that a structure is \definend{closed} under an operation if
performing that operation on its members always yields another member.

\ExecuteMetaData[\catchfilefn]{lem:RegLangsClosedUnderUnionConcatStar}
\begin{proof}
  In the first lemma for the proof of Kleene's Theorem
  we did the regular expression operations of pipe,
  concatenation, and Kleene star.
  Those correspond to set operations;
  for instance, if $R_0$ is a regular expression describing the language
  $\lang_0$, and $R_1$ describes $\lang_1$, then the regular expression
  $\re{$R_0$|$R_1$}$ describes $\lang_0\cup\lang_1$.
\end{proof}
\end{frame}



\begin{frame}
\ExecuteMetaData[\catchfilefn]{th:RegLangsClosedCompIntDiff}

\ExecuteMetaData[\catchfilefn]{pf:RegLangsClosedCompIntDiff}

\pause
\begin{example}
Let $\Sigma=\B=\set{\str{0},\str{1}}$.
We could show that the language
$\lang=\set{\sigma\str{1}\suchthat \sigma\in\kleenestar{\Sigma}}$
is regular by producing a Finite State machine to accept it,
or by giving a regular expression that describes it.
But we can also use closure.
That language is the concatenation $\lang=\lang_0\concat\lang_1$
of
$  \lang_0=\kleenestar{\Sigma}$
and
$  \lang_1=\set{\str{1}}$,
which are both clearly regular.
Regular languages are closed under concatenation, so $\lang$ is regular.
\end{example}
\end{frame}


\begin{frame}
\begin{example}
The language 
\begin{equation*}
\lang=\set{\sigma\in\kleenestar{\set{\str{a},\str{b},\str{c}}}
  \suchthat \text{$\sigma$ does not have the substring \str{acb}}}
\end{equation*}
it the complement of
$\setcomp{\lang}=\set{\sigma\in\kleenestar{\set{\str{a},\str{b},\str{c}}}
  \suchthat \text{$\sigma$ has the substring \str{acb}}}$.
The language $\setcomp{\lang}$ is regular because it is 
described by a regular expression,
\re{(a|b|c)*acb(a|b|c)*}.
\end{example}

\pause
\begin{example}
We can show that the language 
\begin{equation*}
\lang=\set{\sigma\in\kleenestar{\set{\str{a},\str{b}}}
  \suchthat \text{$\sigma$ contains both of the substrings \str{acb} and \str{cbc}}}
\end{equation*}
is regular by constructing a Finite State machine
for each substring or give regular expressions, and then cite that the regular
languages are closed under intersection.
(By the way, note that
the string $\sigma=\str{acbc}$ is a member of $\lang$.)
\end{example}
\end{frame}


% ========================================================
\section{Non-regular languages}

\begin{frame}{Periodicity}
\ExecuteMetaData[\catchfilefn]{ex:FSMLoopingi}
\begin{center}
  \includegraphics{\asydir nfsm/nfsm58.pdf}
\end{center}
\ExecuteMetaData[\catchfilefn]{ex:FSMLoopingii}
\end{frame}

\begin{frame}{Pumping lemma}
\ExecuteMetaData[\catchfilefn]{th:PumpingLemma}

\proofname.\hspace{0.6em}
\ExecuteMetaData[\catchfilefn]{pf:PumpingLemmai}
\end{frame}

\begin{frame}
\ExecuteMetaData[\catchfilefn]{pf:PumpingLemmaii}\qedsymbol
\end{frame}
\begin{frame}{Using the Pumping Lemma}
When we use the Pumping Lemma, it is usually to show that a language
is not regular.
\begin{example}
Show that each language over $\Sigma=\set{\str{a},\str{b}}$ is not a regular:
\begin{enumerate}
\item $\set{\str{a}^n\str{b}^n\suchthat n\in\N}$

\pause
\item $\set{\str{a}^n\str{b}^m\suchthat n=m+1}$

\pause
\item the set of palindromes.
\end{enumerate}
\end{example}
\end{frame}




% ========================================================
\section{Pushdown machines}
\begin{frame}{Introduction}
\ExecuteMetaData[\catchfilefn]{discussion:StackIntroi}

\ExecuteMetaData[\catchfilefn]{discussion:StackIntroii}
\begin{center}
  \vcenteredhbox{\includegraphics[height=1.1in]{../../automata/pix/dishes.jpg}}
  \hspace{4em}
  \vcenteredhbox{\includegraphics{\asydir machine/machine02.pdf}}
  \hspace{1.25em}
  \vcenteredhbox{\includegraphics{\asydir machine/machine03.pdf}}
  \hspace{1.25em}
  \vcenteredhbox{\includegraphics{\asydir machine/machine04.pdf}}
  \hspace{1.25em}
  \vcenteredhbox{\includegraphics{\asydir machine/machine05.pdf}}
\end{center}
\end{frame}

\begin{frame}
\ExecuteMetaData[\catchfilefn]{def:PushdownMachine}
\begin{center}
  \includegraphics{\generalasydir share/machine03.pdf}
\end{center}
The book has the full details.
We will give a couple of examples.
\end{frame}

\begin{frame}
\begin{example}
\ExecuteMetaData[\catchfilefn]{ex:BalancedParensAcceptedPushdown}
\end{example}
\end{frame}


\begin{frame}
\begin{tapesequence}
  \begin{tapesequencetable}
    0 &\tapegraphic{../../automata/asy/machine/machine06.pdf} \\
    1 &\tapegraphic{../../automata/asy/machine/machine07.pdf} \\
    2 &\tapegraphic{../../automata/asy/machine/machine08.pdf} \\
    3 &\tapegraphic{../../automata/asy/machine/machine09.pdf} \\
    4 &\tapegraphic{../../automata/asy/machine/machine10.pdf} \\
    5 &\tapegraphic{../../automata/asy/machine/machine11.pdf} \\
    6 &\tapegraphic{../../automata/asy/machine/machine12.pdf} \\
    7 &\tapegraphic{../../automata/asy/machine/machine13.pdf} \\
  \end{tapesequencetable}
\end{tapesequence}
\end{frame}


\begin{frame}
\begin{example}
\ExecuteMetaData[\catchfilefn]{ex:PalindromesUsingMiddleMarkeri}
\end{example}
\end{frame}


\begin{frame}
This computation has the machine accept \str{bacab}.
\begin{tapesequence}
  \begin{tapesequencetable}
    0 &\tapegraphic{../../automata/asy/machine/machine25.pdf} \\
    1 &\tapegraphic{../../automata/asy/machine/machine26.pdf} \\
    2 &\tapegraphic{../../automata/asy/machine/machine27.pdf} \\
    3 &\tapegraphic{../../automata/asy/machine/machine28.pdf} \\
    4 &\tapegraphic{../../automata/asy/machine/machine29.pdf} \\
    5 &\tapegraphic{../../automata/asy/machine/machine30.pdf} \\
    6 &\tapegraphic{../../automata/asy/machine/machine31.pdf} \\
  \end{tapesequencetable}
\end{tapesequence}
\end{frame}



\begin{frame}{Nondeterministic pushdown machines}
\ExecuteMetaData[\catchfilefn]{discussion:NondeterminsticPushdown}
\end{frame}


\begin{frame}
\begin{example}
\ExecuteMetaData[\catchfilefn]{ex:NondeterministicPushDownAcceptsPalindromes}
\end{example}
\end{frame}

\begin{frame}
Here the machine
accepts the string \str{0110},
using instructions $0$, $9$, $16$, and~$17$.
\begin{tapesequence}
  \begin{tapesequencetable}
    0 &\tapegraphic{\asydir machine/machine14.pdf} \\
    1 &\tapegraphic{\asydir machine/machine15.pdf} \\
    2 &\tapegraphic{\asydir machine/machine16.pdf} \\
    3 &\tapegraphic{\asydir machine/machine17.pdf} \\
    4 &\tapegraphic{\asydir machine/machine18.pdf} \\
  \end{tapesequencetable}
\end{tapesequence}
\end{frame}
\begin{frame}
Here is the machine accepting \str{01010}.
\begin{tapesequence}
  \begin{tapesequencetable}
    0 &\tapegraphic{\asydir machine/machine19.pdf} \\
    1 &\tapegraphic{\asydir machine/machine20.pdf} \\
    2 &\tapegraphic{\asydir machine/machine21.pdf} \\
    3 &\tapegraphic{\asydir machine/machine22.pdf} \\
    4 &\tapegraphic{\asydir machine/machine23.pdf} \\
    5 &\tapegraphic{\asydir machine/machine24.pdf} \\
  \end{tapesequencetable}
  \end{tapesequence}
\end{frame}


\begin{frame}{Relation between machine types and languages}
Below, the box includes all languages.
That is, fix an alphabet~$\Sigma$ and then
the universe consists of every subset of $\kleenestar{\Sigma}\!$.
\begin{center} \small
  % \mbox{Languages over $\B$:\ }
  \vcenteredhbox{\includegraphics{\asydir pda/pda00.pdf}}
  \hspace{2em}
  \begin{tabular}{rl}
    \tabulartext{Class}  &\tabulartext{Machine type} \\
    \hline
    $A$ &Finite State, including nondeterministic\rule{0em}{2.15ex} \\
    $B$ &Pushdown \\
    $C$ &nondeterministic Pushdown \\
    $D$ &Turing \\
  \end{tabular}
\end{center}
Area~$A$ encloses the languages that are regular, those decided by
some Finite State machine.
Area~$B$ enclosed the languages that are decided by some deterministic
Pushdown machine.
Area~$C$ contains the languages decided by some nondeterministic
Pushdown machine.
And, in area~$D$ are the languages decided by some Turing machine.
\end{frame}
\end{document}




